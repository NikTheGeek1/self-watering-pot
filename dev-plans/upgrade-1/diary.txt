2026-04-16
- Created the Upgrade 1 planning folder, project brief, implementation plan, and root `AGENTS.md`.
  Rationale: establish the requested process artifacts before the firmware refactor so future edits stay aligned with the implementation goal.
- Replaced the single-file firmware with focused modules for config storage, plant control, networking, LEDs, serial commands, and embedded web content.
  Rationale: the Wi-Fi provisioning, OTA, and dashboard features needed clear separation so the watering logic could remain local-first and maintainable.
- Added the Wi-Fi state machine, protected setup AP, LAN dashboard/API, mDNS, ArduinoOTA integration, and `wifi clear`.
  Rationale: this delivers the full Upgrade 1 networking scope while preserving the existing plant behavior and safe boot defaults.
- Updated the README to document the new setup flow, local dashboard, OTA behavior, and serial command surface.
  Rationale: the repository documentation needed to match the implemented provisioning and OTA workflow.
- Moved the status LEDs to `GPIO13` (green) and `GPIO14` (red) in the firmware constants and hardware spec.
  Rationale: align the codebase and wiring documentation with the revised LED pin choice for the actual build.

- Fixed early web server startup so the HTTP server only starts after SoftAP or STA networking is active.
  Rationale: the previously flashed image crashed at boot with an lwIP Invalid mbox assert because WebServer::begin() ran before the network stack was initialized.
- Bumped the firmware version to `1.0.1-ota-test` and added a boot log marker for OTA verification.
  Rationale: make the OTA test image easy to distinguish from the currently flashed serial image during boot and status checks.

- Added `ArduinoOTA.begin()` after successful station connect so OTA actually listens on the configured port.
  Rationale: the firmware previously advertised OTA as available but never started the OTA listener, so Wi-Fi updates could not begin.
- Lowered `upload_speed` in `platformio.ini` to `115200` for reliable USB flashing during recovery and verified an OTA update from `1.0.0-upgrade1` to `1.0.1-ota-test`.
  Rationale: the USB link was unstable at `460800`; lowering the baud rate stabilized recovery flashing and allowed the OTA path to be proven end to end.
[2026-04-16] Added a persisted watering-history buffer with a 5-event cap, wired it into pump start/stop handling, exposed it in /api/status, and rendered it in the dashboard so recent watering runs are visible without adding cloud or streaming dependencies.
[2026-04-16] Guarded history-key cleanup with Preferences.isKey() so saving the first few events does not emit noisy NOT_FOUND errors for empty slots.
[2026-04-16] Added NTP-backed start/end timestamps to each watering event, stored them in Preferences, and updated the dashboard to render when a watering run started and ended in local browser time.
[2026-04-16] Simplified watering history timestamps to keep only the start time plus duration, removed end-time exposure from the API/UI, and scrubbed old persisted end-time keys when saving history.
[2026-04-16] Added a dedicated test inventory file covering unit, functional/integration, and end-to-end hardware scenarios for provisioning, OTA, dashboard, plant control, LEDs, serial commands, persistence, mDNS, NTP, and watering history.
[2026-04-16] Added a dashboard manual-watering button and authenticated `/api/manual-water` route so the UI can trigger one pump pulse using the currently configured pulse duration.
[2026-04-16] Added the Upgrade 1 test implementation plan in `dev-plans/upgrade-1/test-plan.txt` and documented the new native, Playwright, and hardware-smoke execution paths in the README.
[2026-04-16] Added a native PlatformIO test environment, a narrow TimeService clock seam, and a host-side fake Arduino/ESP32 support layer so the current firmware modules can run under GoogleTest without changing runtime behavior.
[2026-04-16] Implemented an initial native test suite covering ConfigStore, TimeService, PlantController, NetworkManager, LedManager, SerialConsole, and the generated web content.
[2026-04-16] Added a Playwright harness that serves the real dashboard/provisioning HTML from `web_content.cpp`, plus browser tests for auth, settings, manual watering, provisioning, polling, and watering-history rendering.
[2026-04-16] Added script-assisted hardware smoke tooling and a GitHub Actions workflow so firmware build, native tests, and Playwright tests can run consistently in local development and CI.
[2026-04-16] Expanded the native suite with the next batch of coverage: persisted PlantController startup state, calibration capture/clear, setter clamping, automatic watering behavior, OTA-lock pump stop behavior, reconnect/mDNS and provisioning cases in NetworkManager, more status/settings/calibration API cases, and broader SerialConsole command coverage.
[2026-04-16] Expanded the Playwright suite with the next dashboard cases: initial form hydration, manual-watering success feedback, empty-history rendering, and calibration-button flows.
[2026-04-16] Verified the expanded test batch locally: `pio test -e native` now passes with 39 native tests, and `npm run test:ui` passes with 10 Playwright tests.
[2026-04-16] Expanded the next test batch with the remaining core LED patterns, deeper NetworkManager route and OTA-state cases, and more SerialConsole coverage including aliases, diagnostics, and invalid auto-on behavior.
[2026-04-16] Expanded the Playwright suite again with additional watering-history ordering checks and another manual-watering/dashboard interaction pass.
[2026-04-16] Verified the next batch locally: `pio test -e native` now passes with 53 native tests, `npm run test:ui` passes with 12 Playwright tests, and `python3 scripts/test_hardware/smoke.py manual-water` succeeded against the live device at `smart-pot.local`.
[2026-04-16] Expanded another host-side batch with more ConfigStore and TimeService assertions, more automatic-watering branches in PlantController, and additional NetworkManager auth, OTA, setup-mode, and credential-clearing cases.
[2026-04-16] Expanded the Playwright harness to mirror the firmware’s invalid auto-enable behavior and added another dashboard error-path test for that banner flow.
[2026-04-16] Added a safe hardware smoke `status-schema` command and verified it against the live device. The current totals are `68` passing native tests, `13` passing Playwright tests, and the live smoke commands for `status-schema` and earlier `manual-water` both succeeding against `smart-pot.local`.
