## Agent Instructions

- Read `dev-plans/upgrade-1/project-brief.txt` and `dev-plans/upgrade-1/dev-plan.txt` before making code changes so the implementation stays aligned with the upgrade goal.
- Update `dev-plans/upgrade-1/diary.txt` after every meaningful code change. Record what changed and the rationale behind it.
- Keep responsibilities separated across organized folders and files so the codebase stays easy to navigate.
- Prefer files under 500 lines when practical. This is a soft guideline, not a hard limit.
